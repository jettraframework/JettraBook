# Capítulo 22


En este capítulo, vamos a explorar la implementación de JmoordbCore con JettraFramework

Este capítulo incluye los temas:


Pasos:

* Producer debe implementar la interface JettraConfig
* @AutosecuenceRepository debe utilizar configEngine JETTRA_CONFIG (entity = Autosequence.class, configEngine = ConfigEngine.JETTRA_CONFIG)
* Todos los repositorios deben utilizar configEngine JETTRA_CONFIG (entity = Autosequence.class, configEngine = ConfigEngine.JETTRA_CONFIG)

## Producer


```java

import com.jettraserver.config.JettraConfig;
import com.jmoordb.core.annotation.DateSupport;
import com.jmoordb.core.annotation.enumerations.JakartaSource;
import com.mongodb.client.MongoClient;
import com.mongodb.client.MongoClients;
import jakarta.enterprise.context.ApplicationScoped;
import jakarta.enterprise.inject.Disposes;
import jakarta.enterprise.inject.Produces;
import java.io.Serializable;

@ApplicationScoped
@DateSupport(jakartaSource = JakartaSource.JAKARTA)
public class MongoDBProducer implements Serializable, JettraConfig {

//    @Inject
//    private Config config;
//    @Inject
//    @ConfigProperty(name = "mongodb.uri")
//    private String mongodburi;
private String mongodburi =getMicroprofileConfig("mongodb.uri");


    @Produces
    @ApplicationScoped
    public MongoClient mongoClient() {      
        MongoClient mongoClient = MongoClients.create(mongodburi);
       return mongoClient;

    }

    public void close(@Disposes final MongoClient mongoClient) {
        mongoClient.close();
    }

}


```

## AutogeneratedRepository

```java

@AutosecuenceRepository(entity = Autosequence.class, configEngine = ConfigEngine.JETTRA_CONFIG)
public interface AutogeneratedRepository {

    @Autogenerated()
    public Long generate(String database, String collection);

}
```



## History

```java
@Repository(entity = History.class, database = "{mongodb.databasehistory",configEngine = ConfigEngine.JETTRA_CONFIG)
public interface HistoryRepository extends CrudRepository<History, Long>{
           @Lookup
public List<History> lookup(Search search);
  @Count()
    public Long count(Search... search);
}
```


## CountryRepository

```java
import com.avbravo.jettraserverhelloworld.model.Country;
import com.jmoordb.core.annotation.enumerations.CaseSensitive;
import com.jmoordb.core.annotation.enumerations.ConfigEngine;
import com.jmoordb.core.annotation.enumerations.LikeByType;
import com.jmoordb.core.annotation.enumerations.TypeOrder;
import com.jmoordb.core.annotation.repository.Count;
import com.jmoordb.core.annotation.repository.CountLikeBy;
import com.jmoordb.core.annotation.repository.LikeBy;
import com.jmoordb.core.annotation.repository.Lookup;
import com.jmoordb.core.annotation.repository.Repository;
import com.jmoordb.core.model.Search;
import com.jmoordb.core.repository.CrudRepository;
import java.util.List;

/**
 *
 * @author avbravo
 */
@Repository(entity = Country.class,configEngine = ConfigEngine.JETTRA_CONFIG)
public interface CountryRepository extends CrudRepository<Country, String> {

//    @Find 
//    public List<Country> findAll();
      
    @Lookup
    public List<Country> lookup(Search search);

    @Count()
    public Long count(Search... search);
    
     @CountLikeBy(caseSensitive = CaseSensitive.NO, likeByType = LikeByType.ANYWHERE)
    public Long countLikeByCountry(String country);

    @LikeBy(caseSensitive = CaseSensitive.NO, typeOrder = TypeOrder.ASC, likeByType = LikeByType.ANYWHERE)
    public List<Country> likeByCountry(String country);
}


```

## Resumen






