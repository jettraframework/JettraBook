# Capítulo 5

Este capítulo se centra en la implementación de campos autoincrementables en MongoDB, utilizando la anotación @Id.

Este capítulo incluye los temas:

* Campos Incrementables

* OpenAPI


## Campos Incrementables

   
Jmoordbcore implementa un mecanismo para generar valores incrementales mediante el uso de una colección que almacena el contador y estos se generan al utilizar el método findOneAndUpdate() de MongoDB.

Para implementar valores incrementales, debe crear una interfaz repositorio llamada AutogenerateRepository. Esta interfaz debe contener un único método llamado **generate**, al cual se le debe aplicar la anotación @Autogenerated().

La generación automática de valores incrementables está disponible para entidades cuyo campo clave es de tipo Long y la anotación @Id es (autogeneratedActive  = GenerationType.AUTO).


JmoordbCore ofrece la interface



```java
@Tt(ElementType.TYPE)
@Retention(RetentionPolicy.SOURCE)
public @interface AutosecuenceRepository {
    Class<?> entity();
    JakartaSource jakartaSource() default JakartaSource.JAKARTA;
    ConfigEngine configEngine() default ConfigEngine.MICROPROFILE_CONFIG;
    String collection() default "autosecuence";
    /**
     * database_name --> Es un nombre de base de datos que indique el
     * desarrollador {mongodb.database} --> Es el parametro en
     * Microorofile-config.properties
     *
     * @return
     */
    String database() default "{mongodb.jmoordb}";
     String commentary() default "";

}

```

**Nota:**

* Es recomendable utilizar MICROPROFILE_CONFIG para servidores Jakarta EE/Eclipse Microprofile tales como Helidon, Payara, Wildfly,OpenLiberty, Quarkus, TommEE.

* Para JettraServer se recomienda establecer JETTRA_CONFIG como motor para trabajar con el archivo de configuración.




Ejemplo de AutogeneratedRepository:
   
```java

import com.jmoordb.core.annotation.enumerations.JakartaSource;
import com.jmoordb.core.model.Autosequence;
import com.jmoordb.core.annotation.autosecuence.Autogenerated;
import com.jmoordb.core.annotation.autosecuence.AutosecuenceRepository;

@AutosecuenceRepository(entity = Autosequence.class)
public interface AutogeneratedRepository {
  @Autogenerated() 
  public Long generate(String database,String collection);
}

```



 
Jmoordbcore generará una nueva colección denominada 'autosequence' en la base de datos indicada en la propiedad **mongodb.jmoordb** del archivo microprofile-config.properties. Esta colección almacenará la secuencia de los documentos generados en la misma, tal como se muestra en el siguiente ejemplo:

```

autosequence{
       databasecollection:"ejemplodb_pais",
       sequence: 1


}


```

Ejemplo de entidad con autoincremetable denominada Pais:

```java

@Entity()
public class Pais {
    @Id(strategy = GenerationType.AUTO)
    private Long idpais;
    @Column
    private String pais; 
    @Column
    private Date fecha;
// constructor
// set/get
}


```
 
Defina el repositorio para la entidad Pais:


 
```java

@Repository(entity = Pais.class)
public interface PaisRepository extends CrudRepository<Pais, Long>{
    
}


```

Comience por crear la clase PaisController. En esta clase, vamos a añadir la documentación de los métodos utilizando OpenAPI. Todo lo que necesita hacer es agregar las anotaciones correspondientes y proporcionar las descripciones requeridas. Observe el siguiente ejemplo:


```java
@Path("pais")
@Tag(name = "Información del pais", description = "Endpoint para entidad Pais")
public class PaisController {
  @Inject
  PaisRepository paisRepository;

  @GET
  @Produces({MediaType.APPLICATION_XML, MediaType.APPLICATION_JSON})
  public List<Pais> findAll() {
     return paisRepository.findAll();
  }


  @GET
  @Path("{idpais}")
  @Operation(summary = "Busca un pais por el idpais", description = "Busqueda de pais por idpais")
  @APIResponse(responseCode = "200", description = "El pais")
  @APIResponse(responseCode = "404", description = "Cuando no existe el idpais")
  @APIResponse(responseCode = "500", description = "Servidor inalcanzable")
  @Tag(name = "BETA", description = "Esta api esta en desarrollo")
  @APIResponse(description = "El pais", content = @Content(mediaType = MediaType.APPLICATION_JSON, schema = @Schema(implementation = Pais.class)))
  public Pais findByIdpais(
         @Parameter(description = "El idpais", required = true, example = "1", schema = @Schema(type = SchemaType.NUMBER)) @PathParam("idpais") Long idpais) {
      return paisRepository.findByPk(idpais).orElseThrow(
              () -> new WebApplicationException("No hay pais con idpais " + idpais, Response.Status.NOT_FOUND));
  }


  @POST
  @Operation(summary = "Inserta un nuevo pais", description = "Inserta un nuevo pais")
  @APIResponse(responseCode = "201", description = "Cuanoo se crea un  pais")
  @APIResponse(responseCode = "500", description = "Servidor inalcanzable")
  @Tag(name = "BETA", description = "Esta api esta en desarrollo")
  public Response save(
          @RequestBody(description = "Crea un nuevo pais.", content = @Content(mediaType = "application/json", schema = @Schema(implementation = Pais.class))) Pais pais) {
       pais.setFecha(new Date());
      return Response.status(Response.Status.CREATED).entity(paisRepository.save(pais)).build();
    }


  @PUT
  @Operation(summary = "Inserta un nuevo pais", description = "Inserta un nuevo pais")
  @APIResponse(responseCode = "201", description = "Cuanoo se crea un  pais")
  @APIResponse(responseCode = "500", description = "Servidor inalcanzable")
  @Tag(name = "BETA", description = "Esta api esta en desarrollo")
  public Response update(
         @RequestBody(description = "Crea un nuevo pais.", content = @Content(mediaType = "application/json", schema = @Schema(implementation = Pais.class))) Pais pais) {
      pais.setFecha(new Date());
     return Response.status(Response.Status.CREATED).entity(paisRepository.update(pais)).build();
  }


  @DELETE
  @Path("{idpais}")
  @Operation(summary = "Elimina un pais por  idpais", description = "Elimina un pais por su idpais")
  @APIResponse(responseCode = "200", description = "Cuando elimina el pais")
  @APIResponse(responseCode = "500", description = "Servidor inalcanzable")
  @Tag(name = "BETA", description = "Esta api esta en desarrollo")
  public Response delete(
         @Parameter(description = "El elemento idpais", required = true, example = "1", schema = @Schema(type = SchemaType.NUMBER)) @PathParam("idpais") Long idpais) {
       paisRepository.deleteByPk(idpais);
       return Response.status(Response.Status.NO_CONTENT).build();
  }    
}

```

Para construir el proyecto, utilice los siguientes comandos Maven en la consola:


```shell

mvn clean verify payara-micro:bundle

```

Ejecute el proyecto utilizando los siguientes comandos:

```shell

java -jar target/capitulo02-microbundle.jar --noHazelcast


```

Siga estos pasos para insertar documentos en la colección 'pais' utilizando Curl:


```shell
curl --location --request POST 'http://localhost:8080/api/pais/' --header 'Content-Type: application/json' --data-raw '{"idpais": 5, "pais": "Estados Unidos de America"}'


curl --location --request POST 'http://localhost:8080/api/pais/' --header 'Content-Type: application/json' --data-raw '{"idpais": 6, "pais": "Panamá"}'


curl --location --request POST 'http://localhost:8080/api/pais/' --header 'Content-Type: application/json' --data-raw '{"idpais": 7, "pais": "Colombia"}'


```

Como se detalla en los capítulos previos, inicie sesión en la consola de MongoDB que se ejecuta en un contenedor Docker. A continuación, realice una consulta utilizando el método find().

```shell
> db.pais.find()                                                                                             
{ "_id" : ObjectId("63485e883938025f05c44283"), "idpais" : NumberLong(5), "pais" : "Estados Unidos de America", "fecha" : ISODate("2022-10-13T18:52:56.876Z") }
{ "_id" : ObjectId("63485ead3938025f05c44284"), "idpais" : NumberLong(6), "pais" : "Panamá", "fecha" : ISODate("2022-10-13T18:53:33.194Z") }
{ "_id" : ObjectId("63485eb83938025f05c44285"), "idpais" : NumberLong(7), "pais" : "Colombia", "fecha" : ISODate("2022-10-13T18:53:44.809Z") }


```

Desde su navegador Web, consola o Postman acceda al servicio Rest mediante el URL [http://localhost:8080/api/pais](http://localhost:8080/api/pais)

La respuesta obtenida incluye los documentos de la colección país. Observe que la fecha se devuelve en formato UTC.

```json

[
 {
  "fecha":"2022-10-13T18:52:56.876Z[UTC]","idpais":5,"pais":"Estados Unidos de America"
 },
 {
  "fecha":"2022-10-13T18:53:33.194Z[UTC]","idpais":6,"pais":"Panamá"
 },
 {
  "fecha":"2022-10-13T18:53:44.809Z[UTC]","idpais":7,"pais":"Colombia"
 }
]


```

## OpenAPI Ejemplo


En el Capítulo 2, configuramos el proyecto para integrar OpenAPI. En este capítulo, proporcionaremos un ejemplo utilizando la entidad 'País'. Para acceder a la información de los endpoints, utilizamos la ruta: [http://localhost:8080/openapi](http://localhost:8080/openapi). El siguiente fragmento de código muestra una parte de la información generada:

```
openapi: 3.0.0
info:
  title: RESTful API
  description: capitulo 02
  contact:
    name: AVbravo
    url: https://avbravo.blogspot.com
    email: avbravo@gmail.com
  version: 1.0.0-Snapshot
servers:
- url: http://localhost:8080/
  description: 'Local Development Server '
tags:
- name: Información del pais
  description: Endpoint para entidad Pais
- name: BETA
  description: Esta api esta en desarrollo
paths:
  /api/jakartaee9:
    get:
      operationId: ping
      responses:
        default:
          content:
            '*/*':
              schema:
                type: object
          description: Default Response.
  
```



## Resumen

En este capítulo, examinamos cómo Jmoordbcore maneja campos incrementales. En el próximo capítulo, profundizaremos en Jmoordbcore maneja fechas utilizando Jakarta RESTful Web Services.